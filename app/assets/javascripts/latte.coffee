# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/
# $('#latteModal').on('hidden.bs.modal', ->
#     console.log("hiddened")
#     $('#latte').html("<%= escape_javascript(render 'layouts/new_latte')) %>")
# )
# $('#latte_btn').click(function () {
#   $('#latte').html("#{escape_javascript(render 'layouts/new_latte', data: current_user.latte_account)}")
# });
  #
  # $("#latte_btn").k = ->
  #   console.log("clicked")
  #   $('#latte').html("#{escape_javascript(render 'layouts/new_latte', data: current_user.latte_account)}")
