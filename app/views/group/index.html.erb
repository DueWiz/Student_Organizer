<p>
<div class="card link-to-text-color" style="border-radius:20px;width:100%;">
  <div class="card-header" style="background-color: #2c3e50; border-top-left-radius:0.5em; border-top-right-radius:0.5em; margin:0px;">
    <div class="row">
    <div class = "col-sm-5">
    <h8>Classes</h8><br />
    <h8>Projects</h8>
    </div>
    <div class="col-sm-7">
      <%= form_tag(group_search_path, method: "get", role: "form", class: "form", id: "groupSearch") do %>
      <%= text_field_tag :name, params[:name], onchange: "$('#groupSearch').submit();", class: "form-control input-xs", placeholder: "Search...", style: "border-radius:15px; height: 35px;" %>
      <% end %>
    </div>
  </div>
  </div>
  <div class="card-block" style="padding:10px;">
    <table class="table table-condense table-hover" style="border-spacing: 0px 0px;">
      <tr>
        <td><%= link_to 'Personal Assignments', groupshow_path(-1)%></td>
      </tr>
        <td style="background-color: #2c3e50;">Classes</td>
        <% user_groups.each do |group| %>
          <% if group.public == true %>
            <tr>
              <td><%= link_to group.year.to_s + "-" + group.term + "-" + group.name + "-" + group.section.to_s, groupshow_path(group) %></td>
            </tr>
          <% end %>
        <% end %>
      <tr>
        <td style="background-color: #2c3e50;">Projects</td>
        <% user_groups.each do |group| %>
          <% if group.public == false%>
            <tr>
              <td><%= link_to group.year.to_s + "-" + group.term + "-" + group.name + "-" + group.section.to_s, groupshow_path(group) %></td>
            </tr>
          <% end %>
        <% end %>
      </tr>
    </table>
    <br/>
    <div style = "padding:10px;">
      <button type="button" class="btn btn-sm btn-success float-shadow"  data-toggle="modal" data-target="#groupModal" style = "background-color: #18bc9c;">New Class/Project</button>
    </div>
  </div>
</div>
